# ============================================================================
# Configuração da API Backend - Dashboard de Chamados
# ============================================================================
# IMPORTANTE: NÃO commitar arquivo .env real com credenciais!

# ============================================================================
# SUPABASE (Fonte de Dados)
# ============================================================================
# URL do seu projeto Supabase (Dashboard → Settings → API → Project URL)
SUPABASE_URL=https://seu-projeto.supabase.co

# Anon Key (para operações públicas de leitura)
# Dashboard → Settings → API → Project API keys → anon (public)
SUPABASE_KEY=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...

# NOTA: O sync Drive → Supabase agora é feito pela Edge Function
# Não precisa mais de configurações do Google Drive aqui!

# ============================================================================
# API Flask
# ============================================================================
# Porta do servidor (padrão: 5001)
PORT=5001

# Ambiente (development ou production)
FLASK_ENV=development
FLASK_DEBUG=False

# CORS - Origens permitidas (separadas por vírgula)
CORS_ORIGINS=http://localhost:8080,http://127.0.0.1:8080,http://localhost:3000

# Cache de dados (em segundos) - 300s = 5 minutos
CACHE_TIMEOUT=300
